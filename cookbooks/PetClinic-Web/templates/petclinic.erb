server {
    listen 80;
    server_name <%= @WEB_dns %>;  # Your custom IP or domain

    root /home/ubuntu/spring-petclinic-angular/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
